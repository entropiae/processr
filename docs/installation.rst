.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install processr

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv processr
    $ pip install processr
